"""Plugin-tier support code (the declarative YAML compiler). Out-of-tree
plugins themselves live under <APP_DIR>/plugins/ and are loaded by
core/discovery.py — this package is core code that ships with the app."""
